class MovieBuzzModal {
  String title, image, profilePic, time, favCount;

  MovieBuzzModal({
    required this.title,
    required this.image,
    required this.profilePic,
    required this.favCount,
    required this.time,
  });
}
